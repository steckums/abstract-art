<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.software.live_wallpaper" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbstractArt"
        tools:targetApi="31">

        <service
            android:exported="true"
            android:label="@string/app_name" android:name=".Wallpaper" android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data android:name="android.service.wallpaper" android:resource="@xml/metadata" />
        </service>

        <activity
            android:label="@string/wallpaper_settings"
            android:name="com.steckums.abstractart.Settings"
            android:exported="true"
            tools:ignore="ExportedPreferenceActivity" />

        <activity
            android:label="@string/settings.background_chooser"
            android:screenOrientation="portrait"
            android:name="com.steckums.abstractart.settings.BackgroundSelector"
            android:theme="@android:style/Theme.NoTitleBar"
            android:exported="false" />

        <activity
            android:label="title"
            android:name="com.steckums.abstractart.ServiceDialog"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />
    </application>

</manifest>